import type { StepKey } from "./types";

export const STEP_PATH: Record<StepKey, string> = {
  shopify: "/onboarding/shopify",
  productos: "/onboarding/productos",
  numeros: "/onboarding/numeros",
  meta: "/onboarding/meta",
  "meta-cuentas": "/onboarding/meta/cuentas",
  listo: "/onboarding/listo",
};
